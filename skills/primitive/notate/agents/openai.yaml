interface:
  display_name: "Notate"
  short_description: "Create an internal primitive skill from a real case"
  default_prompt: "Use pm:notate to create an internal primitive skill from this real case."
policy:
  allow_implicit_invocation: false
